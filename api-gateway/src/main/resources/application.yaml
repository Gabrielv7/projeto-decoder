server:
  port: 8080

spring:
  application:
    name: ead-api-gateway
  cloud:
    gateway:
      routes:
      - id: ead-authuser
        uri: lb://EAD-AUTHUSER
        predicates:
          - Path=/ead-authuser/**
      - id: ead-course
        uri: lb://EAD-COURSE
        predicates:
          - Path=/ead-course/**

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost